# Existing environment variables
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key
CLERK_SECRET_KEY=your_clerk_secret_key
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

# Salesforce OAuth Configuration
# Create a Connected App in Salesforce Setup:
# 1. Go to Setup > App Manager > New Connected App
# 2. Enable OAuth Settings
# 3. Set Callback URL to: http://localhost:3000/api/salesforce/oauth/callback (for dev)
# 4. Select OAuth Scopes: api, id, web, refresh_token, offline_access
# 5. Save and copy the Consumer Key and Secret below

SF_OAUTH_CLIENT_ID=your_salesforce_consumer_key
SF_OAUTH_CLIENT_SECRET=your_salesforce_consumer_secret

# Your application URL (update for production)
NEXT_PUBLIC_APP_URL=http://localhost:3000